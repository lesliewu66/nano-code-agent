"""Memory and persistence modules for RouteAgent.

This directory is reserved for future memory backends. As tutorial chapters
add persistence capabilities, add modules here following the pattern:

- `conversation.py` — Conversation history storage
- `task_store.py` — Task persistence
- `vector_store.py` — Vector-based memory (for RAG features)

Each module should expose a consistent interface for the agent to use.
"""
